default:
  autoload:
    '': "%paths.base%/phs/contexts"
  suites:
    default:
      contexts:
        - \phs\tests\phs\contexts\phs_common_context
      paths:
        - "%paths.base%/behat/features"

includes:
  - "%paths.base%/behat/plugins.yml"
